# trading_dashboard Package

Importable dashboard-side adapters live here.

## Modules

- `read_models.py` reads accepted storage-hosted dashboard read-model `latest.json` files and returns UI-ready view dictionaries without querying raw manager/model/data/execution/storage internals.
