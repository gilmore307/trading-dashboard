# deploy

Deployment templates for running the read-only trading dashboard UI as host-managed infrastructure.

This directory owns service/template files only. It does not own runtime state, generated dashboard read models, provider credentials, or workflow control logic.
